module.exports = {
  // Use single quotes for strings instead of double quotes
  singleQuote: true,
  // Place the closing bracket of JSX elements on the same line as the opening tag's last attribute
  bracketSameLine: true,
  // Print trailing commas whenever possible, especially for multi-line code
  trailingComma: 'all',
  // Specify the line length that Prettier will try to wrap at
  printWidth: 80,
  // Specify the number of spaces per indentation level
  tabWidth: 2,
  // Use semicolons at the end of statements
  semi: true,
  // Include parentheses around a sole arrow function parameter
  arrowParens: 'avoid',
  // Ensure consistent line endings across different operating systems
  endOfLine: 'auto',
};
